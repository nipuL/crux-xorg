# Description: X Input client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: xorg-libxi, xorg-xproto-compat32, xorg-libx11-compat32, xorg-xextproto-compat32, xorg-libxext-compat32, xorg-inputproto-compat32

name=xorg-libxi-compat32
version=1.2.1
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libXi-$version.tar.bz2)

build() {
    cd libXi-$version
    
    ./configure --prefix=/usr \
	--libdir=/usr/lib32 \
	--mandir=/usr/man
    
    make
    make DESTDIR=$PKG install

    rm -rf $PKG/usr/man
}

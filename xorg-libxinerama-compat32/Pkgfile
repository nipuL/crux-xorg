# Description: X Xinerama client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Depends on: xorg-libxinerama, xorg-libx11-compat32, xorg-xextproto-compat32, xorg-libxext-compat32, xorg-xineramaproto-compat32

name=xorg-libxinerama-compat32
version=1.0.3
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libXinerama-$version.tar.bz2)

build() {
    cd libXinerama-$version

    ./configure --prefix=/usr \
	--libdir=/usr/lib32 \
	--mandir=/usr/man
    
    make
    make DESTDIR=$PKG install

    rm -rf $PKG/usr/man
}

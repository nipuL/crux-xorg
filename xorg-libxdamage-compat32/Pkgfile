# Description: X Damage extension client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: xorg-libxdamage, xorg-libx11-compat32, xorg-fixesproto-compat32, xorg-libxfixes-compat32, xorg-xextproto-compat32, xorg-damageproto-compat32

name=xorg-libxdamage-compat32
version=1.1.1
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libXdamage-$version.tar.bz2)

build() {
	cd libXdamage-$version

	./configure --prefix=/usr --libdir=/usr/lib32

	make
	make DESTDIR=$PKG install
	
	rm -rf $PKG/usr/include
}

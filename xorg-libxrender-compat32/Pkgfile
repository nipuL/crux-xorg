# Description: X Render extension client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: xorg-libxrender, xorg-libx11-compat32, xorg-renderproto-compat32

name=xorg-libxrender-compat32
version=0.9.4
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libXrender-$version.tar.bz2)

build() {
	cd libXrender-$version

	./configure --prefix=/usr --libdir=/usr/lib32

	make
	make DESTDIR=$PKG install
	
	rm -rf $PKG/usr/include
}
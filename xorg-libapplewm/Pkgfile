# Description: xorg lib libAppleWM
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Depends on: xorg-libx11, xorg-libxext, xorg-xextproto, xorg-applewmproto

name=xorg-libapplewm
version=1.0.0
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libAppleWM-$version.tar.bz2)

build() {
	cd libAppleWM-$version

	./configure --prefix=/usr

	make
	make DESTDIR=$PKG install
}

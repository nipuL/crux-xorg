# Description: XCB X protocol descriptions
# URL: http://xcb.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: xorg-xcb-proto

name=xorg-xcb-proto-compat32
version=1.3
release=1
source=(http://xcb.freedesktop.org/dist/xcb-proto-$version.tar.bz2)

build() {
	cd xcb-proto-$version

	./configure --prefix=/usr --libdir=/usr/lib32

	make
	make DESTDIR=$PKG install
	
	rm -rf $PKG/usr/{share,lib}
}

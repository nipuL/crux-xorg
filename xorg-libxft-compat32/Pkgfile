# Description: X font rendering client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: xorg-libxft, xorg-libxrender-compat32, freetype-compat32, fontconfig-compat32

name=xorg-libxft-compat32
version=2.1.13
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libXft-$version.tar.bz2)

build() {
	cd libXft-$version

	./configure --prefix=/usr --mandir=/usr/man --libdir=/usr/lib32

	make
	make DESTDIR=$PKG install
	
	mv $PKG/usr/bin/xft-config $PKG/usr/bin/xft-config32
	rm -rf $PKG/usr/{include,man}
}

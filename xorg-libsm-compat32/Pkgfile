# Description: X Session Management client library
# URL: http://xorg.freedesktop.org
# Maintainer: Tilman Sauerbeck, tilman at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au
# Depends on: e2fsprogs-compat32, xorg-libsm, xorg-libice-compat32, xorg-xproto-compat32, xorg-xtrans-compat32

name=xorg-libsm-compat32
version=1.1.1
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/libSM-$version.tar.bz2)

build() {
	cd libSM-$version

	./configure --prefix=/usr --libdir=/usr/lib32

	make
	make DESTDIR=$PKG install
	
	rm -rf $PKG/usr/include
}
